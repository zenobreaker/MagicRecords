using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// 휠러에게 부착해서 추가적인 기능을 부여하는 것
public class MemoryDrive : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
